[project]
name = "ai-resume-analyzer-sam"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = [
    "boto3>=1.28.0",
    "google-genai==1.32.0",
]

[dependency-groups]
dev = [
    "ipython>=8.0.0",
    "moto>=4.0.0",
    "mypy>=1.0.0",
    "pre-commit>=3.0.0",
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
    "python-dotenv>=1.0.0",
    "python-lambda-local>=0.1.0",
    "ruff>=0.1.0",
]
